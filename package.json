{
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev --host 0.0.0.0",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs"
  },
  "devDependencies": {
    "nitropack": "latest"
  }
}
